# 메력부 MVP 구현 계획

## 2026-07-14 — 메숭이 체온을 이력서·공유 PNG에 포함

- [x] 실제 이력서 미리보기와 1080×1350 공유 PNG 안에 현재 메숭이 체온·익명 응답 수를 표시한다.
- [x] 설문 제출 뒤 새 PNG가 최신 집계값을 다시 렌더링하도록 immutable cache 정책과 이미지 URL renderer version을 조정한다.
- [x] baseline 36.5℃·응답 대기 상태, 응답 수, 이미지 렌더·다운로드·375px 흐름을 unit/E2E로 검증한다.
- [x] 제품 고지와 결정 기록에서 PNG/이력서 제외 설명을 새 동적 공유 이미지 정책으로 갱신하고 전체 품질 명령을 실행한다.

## 2026-07-14 — 익명 메숭이 체온 설문

- [x] 기존 기명 태그형 기능 UI를 `메숭이 체온` 익명 설문으로 교체한다.
- [x] 보스 경험·숙련도(각 -2~+2), 시간 약속(-1/+1) 세 문항과 1회용·7일 만료 초대 링크를 구현한다.
- [x] 캐릭터 OCID별 36.5℃ 기본값에 설문 합계를 누적하고, 새 메력서·버전에서도 같은 온도를 조회한다.
- [x] 공개 검증 페이지에 숫자·게이지·응답 수만 표시하고, 설문자 신원·자유 댓글은 저장하거나 노출하지 않는다. 개별 점수는 익명 집계에만 사용하며 공개하지 않는다.
- [x] 설문 익명성, 초대장 재사용 차단, 캐릭터 간 분리, 보스별 새 메력서 간 온도 유지, 모바일 접근성 및 전체 품질 명령을 검증한다.

## 2026-07-14 — 나의 이력서 목록

- [x] 브라우저에 남아 있는 edit token cookie만으로 소유한 메력서를 식별하는 읽기 전용 목록 API를 만든다.
- [x] 캐릭터명·희망 보스·주기·작성/갱신 시각·공개 상태를 보여 주는 모바일 우선 `나의 이력서` 탭을 추가한다.
- [x] 목록에서 공개 보기, 수정, 기존 내용을 복제해 새 메력서로 저장하는 흐름을 연결한다.
- [x] cookie가 없거나 다른 브라우저인 경우의 빈 상태와 로컬/배포 환경이 분리된다는 안내를 제공한다.
- [x] 권한 경계·목록 정렬·복수 보스 기록·375px 흐름을 테스트하고 모든 품질 명령을 실행한다.

## 2026-07-14 — 메숭이 체온 동행 기록

- [x] `메숭이 체온`을 숫자·별점·순위 없는 기명 동행 기록으로 정의하고, 긍정 태그 1~3개만 허용한다.
- [x] 작성자 edit token으로 7일 만료·1회 사용 동행 확인 링크를 발급하고, 원문 토큰은 링크 발급 응답 외에 저장하지 않는다.
- [x] 평가자는 본인이 관리하는 공개 메력서와 같은 브라우저의 edit token으로만 동행 기록을 제출하게 하며, 자기 평가·중복 평가·만료/재사용 링크를 차단한다.
- [x] Prisma와 mock memory 저장소에 초대장·동행 기록을 저장하고, 공개 검증 화면에는 같은 ResumeVersion에 연결된 기명 태그만 표시한다.
- [x] 동행 기록은 공유 PNG·QR 이미지·이력서 글 복사에는 넣지 않아 immutable version과 content hash를 유지한다.
- [x] API·UI·모바일 E2E·권한·중복/만료 검증을 추가하고, 전체 품질 명령을 실행한다.

## 2026-07-13 — 파티 조건 피드백 반영

- [x] 기존 공개 이력서를 유지한 채 같은 캐릭터·작성 내용을 새 slug의 이력서로 복제 저장할 수 있게 한다.
- [x] 희망 인원을 1~6인격 선택으로 추가하고, 스우는 최대 2인, 최초의 대적자·림보·발드릭스·찬란한 흉성·유피테르는 최대 3인으로 서버와 UI에서 모두 제한한다.
- [x] 가능한 시간에 `요일·시간 지정`, `요일·시간 협의 가능`, `요일·시간 무관` 선택지를 제공하고, 미리보기·PNG·글 복사에 같은 문구를 출력한다.
- [x] 새 작성 내용이 편집기·공개 검증·PNG·plain-text 공유에서 같은 순서로 표시되는지 테스트하고 전체 품질 명령을 실행한다.

## 2026-07-13 — 문서형 메력서와 클립보드 공유

- [x] 1080×1350 정본 PNG를 카드 묶음 대신 표와 구분선 중심의 이력서 용지 레이아웃으로 재구성한다.
- [x] 편집 미리보기도 같은 정보 순서와 `디스코드` 표기를 사용하는 문서형 양식으로 정리한다.
- [x] 공개 메력서의 작성 내용을 일정한 plain-text 양식으로 만들고, 접근 가능한 클립보드 복사 버튼을 제공한다.
- [x] 이미지 캐시 버전·단위 테스트·E2E를 갱신하고 전체 품질 명령을 실행한다.

## 2026-07-13 — 파티 유형 `업적` 추가

- [x] 도메인 enum·입력 검증·저장 JSON에 `ACHIEVEMENT` 파티 유형을 추가한다.
- [x] 작성 화면, 미리보기, 1080×1350 발급 PNG에서 `업적`을 일관되게 표시하고 테스트한다.

## 2026-07-13 — 발급 카드 레이아웃 안정화

- [x] 서버 PNG의 카드 높이와 padding을 고정해 flex 축소로 인한 섹션 겹침을 제거한다.
- [x] 실제 1080×1350 PNG를 시각 확인하고, 공개 화면과 다운로드 PNG의 카드 배치를 동일하게 유지한다.

## 2026-07-13 — 저장 PNG와 공개 이력서 일치

- [x] 공개 이력서 카드와 저장 버튼이 같은 버전의 `/r/[slug]/image?v=` PNG를 참조하도록 정리한다.
- [x] 1080×1350 PNG를 현재 이력서의 다크 헤더·프로필·지원 분야·환산/보스 배율·선택 보스 일러스트·경험/조건·QR 구조로 재구성한다.
- [x] 선택 보스 원본을 서버에서 data URI로 인라인해 PNG에도 누락 없이 표시한다.
- [x] 다운로드 파일명을 `메력서-{slug}-v{version}.png`로 정하고, PNG 시그니처와 1080×1350 크기를 E2E로 검증한다.
- [x] PNG 렌더러 버전을 이미지 URL에 포함해 기존 immutable 캐시와 새 카드 디자인을 분리한다.

## 조사 결과

- 시작 루트는 비어 있었고, 제공된 메력부 키트의 명세를 현재 저장소로 보존했다.
- NEXON API 키가 없는 환경을 기본으로 `mock` provider가 전체 흐름을 지원해야 한다.
- 로컬 개발 환경에서는 PostgreSQL/Prisma를 제공하되, 데모와 테스트는 외부 네트워크 및 별도 DB 없이도 재현 가능해야 한다.

## 구현 체크리스트

- [x] 명세, UX 카피, 아키텍처, 인수 기준, 작업 지시를 검토한다.
- [x] 현재 저장소를 초기화하고 원본 명세 파일을 보존한다.
- [x] Next.js App Router + TypeScript strict + Tailwind + pnpm 기반을 구성한다.
- [x] 도메인 모델, 출처/신선도/버전/길드 관측 규칙과 Zod 검증을 구현하고 단위 테스트를 작성한다.
- [x] mock fixture와 live NEXON adapter 경계를 구현하고 공식 API 매핑을 기록한다.
- [x] 검색 → 작성 → 실시간 미리보기 → 게시 흐름과 edit-token cookie 권한을 구현한다.
- [x] 공개 검증 페이지, stale/expired 처리, 갱신에 따른 immutable version을 구현한다.
- [x] 동일 ResumeVersion 기반의 1080×1350 PNG 및 QR 검증 링크를 구현한다.
- [x] Prisma schema, Docker Compose, seed/migration 명령 및 메모리 데모 저장소 fallback을 구성한다.
- [x] Privacy/Terms/health API, 접근성, 한국어 카피와 README를 완성한다.
- [x] unit/integration/E2E 테스트를 추가하고 모바일 375px 흐름을 검증한다.
- [x] lint, typecheck, test, test:e2e, build를 실행하고 결과를 구현 로그에 기록한다.

## 2026-07-12 — live API·장비 검증 확장

- [x] 최신 공식 문서 기준으로 `character/stat`, `character/item-equipment`, 심볼·캐시 장비·세트 효과 endpoint와 응답 매핑을 기록한다.
- [x] 전투력은 `final_stat`의 API 원값만 보존하고, 현재 장착 전투 장비 전체 및 상세 옵션을 normalized domain model로 확장한다.
- [x] 기본 정보가 정상일 때 능력치·장비 보강 호출이 부분 실패해도 작성 흐름을 유지하고, availability와 안내 문구로 드러낸다.
- [x] 편집기에 주간/월간 보스 구분과 빠른 보스 선택을 추가하고, 사용자가 사용을 허가한 Maple Trackers 원본 보스 일러스트를 서버 어댑터로 적용한다.
- [x] 공개 검증 페이지에 전투력·최종 능력치·장착 전투 장비·심볼·세트 효과·캐시 장비를 출처와 함께 분리 표시한다.
- [ ] 확장된 정규화, 보스 구분, 검색→게시→검증 흐름을 포함해 전체 품질 명령을 다시 실행하고 결과를 기록한다.

## 구현 결정

1. **기본 실행 모드**: API 키가 없으면 자동으로 mock provider를 선택한다. 실 API는 서버 route handler에서만 호출한다.
2. **데모 저장소**: Docker/PostgreSQL/Prisma 구성은 제공하되, 평가와 mock 흐름이 DB 설정 없이 실행되도록 프로세스 메모리 저장소를 기본 fallback으로 둔다. 저장소 인터페이스는 Prisma adapter로 교체 가능하게 유지한다.
3. **이미지**: 서버 route에서 SVG를 PNG로 변환하지 않고, standards-compatible `ImageResponse`로 1080×1350 이미지를 생성한다. QR은 SVG data URL로 포함한다.
4. **버전 URL**: `/r/[slug]?v=N`는 이전 immutable version을 조회할 수 있고, 현재 버전보다 오래되면 최신 버전 안내를 표시한다.

## 완료 판정

`docs/ACCEPTANCE_CRITERIA.md`의 A–I 항목과 다음 명령의 성공을 완료 조건으로 사용한다.

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm build
```

## 2026-07-12 최종 확장 검증

- [x] `pnpm lint`, `pnpm typecheck`, `pnpm test`(4 files, 17 tests), `pnpm test:e2e`(Chromium 2 tests), `pnpm build`를 모두 다시 실행해 통과를 확인했다.
- [x] 검색 편집기에 전투력·전체 최종 능력치·현재 장착 장비를 즉시 표시하고, mock/live provider 상태와 장비 조회 누락 사유를 함께 안내한다.
- [x] E2E는 개발용 3000번 포트를 재사용하지 않고 격리된 3100번 포트와 `.next-e2e` 빌드 디렉터리를 사용한다.

## 2026-07-12 — 메력서 가독성·장비 상세 UX 보완

- [x] 메력서의 핵심 역량 API 지표 묶음을 제거하고, MapleScouter 기준 환산만 별도 작성 내용 항목으로 보인다.
- [x] 환산 입력은 설명·검증 링크를 포함한 별도 편집 섹션으로 분리하고, 공개 메력서와 공유 PNG에는 값과 출처만 표시한다.
- [x] 전체 화면을 고대비 다크 테마로 정리하고, 버튼·입력·출처·신선도 상태의 명암과 키보드 focus 상태를 강화한다.
- [x] 주간/월간 보스 카드는 원본을 자르지 않는 우측 아트와 독립된 텍스트 패널로 바꿔 선택 상태와 내용을 선명하게 표시한다.
- [x] 장비 정보는 실제 API 옵션만을 사용해 아이콘·스타포스·최종/기본/추가/특수 옵션·잠재능력·에디셔널 잠재능력 순서의 고대비 상세 카드로 정리한다.
- [x] 다크·민트 톤의 공유 미리보기 이미지와 Open Graph/Twitter 메타데이터를 추가한다.
- [x] lint, typecheck, unit/E2E 테스트, production build와 Prettier 검사를 다시 실행한다.

## 2026-07-12 — 원격 변경 통합 검증

- [x] 원격 `main`의 최고 전투력 관측, Prisma migration pipeline, 숫자 서식 개선을 보존하면서 화면 개편을 통합한다.
- [x] 중복 환산 필드를 제거하고 `convertedStat` 하나만 저장·검증·편집·미리보기·공유 PNG에 사용한다.
- [x] 편집기와 공개 검증 화면의 장비 상세를 동일한 고대비 확장 카드로 연결한다.
- [x] Prisma Client 생성 후 lint, typecheck, 30개 unit 테스트, Chromium E2E, production build, Prettier, diff 검사를 모두 통과한다.

## 2026-07-13 — 간소화·자동 외부 지표 보완

- [x] 편집기·공개 검증 페이지에서 장비 상세 UI를 제거하고, NEXON 장비 조회/저장 경계는 다른 기능을 깨지 않도록 유지한다.
- [x] 캐릭터 이미지를 정사각형 영역에 더 크게 표시하고 이미지가 있을 때 영역을 채우도록 조정한다.
- [x] 작성 내용 `담당 역할 설명`의 모든 사용자 노출 카피를 `어필 포인트`로 변경한다.
- [x] MapleScouter의 공개 결과 구조와 이용 제약을 확인하고, 자동 수집·비공개 endpoint 호출은 이용약관상 금지되어 구현하지 않는다고 결정한다.
- [x] 외부 값은 닉네임별 참고 링크로만 제공하고, 환산·보스 배율을 복사·추측·임의 계산하지 않는다.
- [ ] 자동 환산·보스 배율은 서면 허가와 문서화된 파트너 API가 제공될 때 별도 server-only adapter로 추가한다.
- [x] lint, typecheck, unit/E2E 테스트, production build를 다시 실행하고 구현 로그를 갱신한다.

## 2026-07-13 — 이력서 작성 경험 재설계

- [x] `환산·보스 배율 참고` 아래에 확인한 환산값과 보스 배율을 작성 내용으로 기록하고, 공개 메력서·공유 PNG에서도 출처를 분명히 표시한다.
- [x] 홈페이지의 `샘플로 체험하기`와 임의 샘플 캐릭터 노출을 제거하고, 실제 캐릭터 검색을 중심으로 빈 공간을 이력서 작성 안내로 재구성한다.
- [x] 홈·편집기·공개 검증 페이지를 종이 이력서/작성 문서의 시각 언어로 통일하되, 모바일 가독성과 키보드 접근성을 유지한다.
- [x] 변경된 필드와 홈페이지 흐름을 단위/E2E 테스트로 보강하고, lint·typecheck·test·test:e2e·build를 재실행한다.

## 2026-07-13 — 브랜드와 작성 정보 표기 정리

- [x] 사용자 노출 제품명을 `메력서`로, 영문 표기를 `RESUMAE`로 통일하고 기존 데이터 식별자·저장소 경로는 호환성을 위해 유지한다.
- [x] 생성형 소셜 썸네일을 `MAPLE-RESUME` 브랜딩으로 새로 만들고 Open Graph/Twitter 메타데이터에 연결한다.
- [x] 캐릭터 스프라이트의 투명 여백을 고려해 웹·공유 PNG의 정사각형 아바타를 시각적으로 꽉 차게 확대한다.
- [x] 화면·문서의 기존 출처 문구를 `작성 내용`으로 통일하되, API 조회값과의 구분은 계속 제공한다.
- [x] 전체 품질 명령과 E2E를 다시 실행하고 구현 로그를 갱신한다.

## 2026-07-13 — Boss-card artwork refinement

- [x] Use Jupiter as the default weekly boss artwork.
- [x] Center both card illustrations inside padded `object-contain` artwork panes.
- [x] Run the regression and full quality checks.

## 2026-07-13 — 편집기 안내 및 결과물 워터마크 보정

- [x] 선택 보강 API 실패로 생성되는 상단 안내 상자를 편집 화면에서 제거한다.
- [x] 실시간 NEXON Open API 안내의 색 대비를 높여 읽기 쉽게 만든다.
- [x] 환산·보스 배율 참고의 파트너 API 안내 문구를 제거한다.
- [x] 편집 미리보기·공개 메력서·1080×1350 PNG 우측 하단에 `크로아/얀보 제작` 워터마크를 표시한다.
- [x] immutable PNG 캐시를 갱신할 수 있도록 다운로드 URL에 템플릿 리비전을 추가하고 전체 품질 명령을 실행한다.

## 2026-07-13 — 보스 선택 제한 및 사이트 워터마크

- [x] 희망 보스의 자유 텍스트 입력을 제거하고, 주간·월간 공식 목록 선택만 허용한다.
- [x] 주기 카드와 드롭다운이 보스명·주기를 원자적으로 함께 선택하도록 하고, API 요청도 목록 밖 보스를 거부한다.
- [x] 워터마크를 메력서·공유 PNG에서 제거하고, 모든 웹 화면의 우측 하단 고정 표기로 옮긴다.
- [x] 375×812 모바일 화면에서 가로 넘침, 보스 선택, 게시 버튼, 고정 워터마크를 실제 점검하고 E2E로 보강한다.
- [x] 전체 품질 명령을 재실행하고 구현 로그에 결과를 기록한다.

## 2026-07-13 — 본문 글꼴 및 브라우저 아이콘

- [x] 참조한 메이플스토리 공지의 본문 서체(`NanumBarunGothic`)를 웹과 공유 PNG에 같은 로컬 글꼴로 적용한다.
- [x] 기존 기본 파비콘을 Resumae의 문서형 브랜드 아이콘으로 교체하고, 브라우저 탭에서 새 아이콘이 우선 선택되게 한다.
- [x] 홈 화면과 모바일 흐름에서 글꼴·파비콘을 확인하고 전체 품질 명령을 재실행한다.

## 2026-07-13 — 미리보기 대비 및 캐릭터 이미지 품질

- [x] 메력서 미리보기의 헤더·푸터 배경과 밝은 글자 색을 일관되게 연결해 WCAG 수준의 읽기 대비를 확보한다.
- [x] 공식 캐릭터 원본을 최근접 보간 없이 표시하고, 과도한 확대를 낮춰 웹 미리보기·공유 PNG의 블록 현상을 줄인다.
- [x] 회귀 테스트와 모바일 E2E, production build를 재실행한다.

## 2026-07-13 — 전체 한글 타이포그래피 안정화

- [x] NanumBarunGothic이 실제로 제공하는 400·700 굵기만 사용하고, 브라우저의 합성 굵기를 차단한다.
- [x] 한글 제목의 과도한 음수 자간을 제거하고 본문·플레이스홀더 색 대비를 WCAG AA 수준으로 보강한다.
- [x] 웹·공유 PNG·375px 모바일 흐름을 포함한 전체 품질 명령을 다시 실행한다.

## 2026-07-13 — 보스 일러스트·지원 분야·참고 지표 정리

- [x] Maple Trackers의 사용자 허가 보스 원본을 키별 정적 자산으로 옮기고, 주기별 공용 대체 이미지를 제거한다.
- [x] 희망 보스명에 포함된 난이도를 단일 기준으로 삼아 별도 난이도 입력·저장·표시를 제거하고 역할·파티 유형의 2열 배치를 유지한다.
- [x] 메력서 미리보기의 환산·보스 배율을 항상 함께 보여 주고, 선택 보스와 같은 일러스트를 남는 영역에 표시한다.
- [x] 보스 키 매핑, 입력 검증, 공개 미리보기, PNG, 375px 모바일 흐름을 포함한 품질 명령을 재실행한다.
