/** Decorative authorship watermark shown above the site, outside each resume artifact. */
export function SiteWatermark() {
  return (
    <p aria-hidden="true" className="site-watermark">
      크로아/얀보 제작
    </p>
  );
}
